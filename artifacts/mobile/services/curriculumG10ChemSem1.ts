/**
 * Re-export shim — the real module now lives in `@workspace/curriculum`.
 * Kept so existing imports of `@/services/curriculumG10ChemSem1` keep working.
 */
export * from '@workspace/curriculum/catalogs/g10ChemSem1';
