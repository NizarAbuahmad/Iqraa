/**
 * Re-export shim — the real module lives in `@workspace/curriculum`.
 * Matches the pattern the other per-semester catalogs use.
 *
 * GENERAL English. The four vocational ESP tracks are a different catalog,
 * `curriculumG10EnglishVocational.ts`.
 */
export * from '@workspace/curriculum/catalogs/g10EnglishSem2';
