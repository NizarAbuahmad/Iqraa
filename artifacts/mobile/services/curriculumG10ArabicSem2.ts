/**
 * Re-export shim — the real module lives in `@workspace/curriculum`.
 * Matches the other per-book shims so app-side imports stay uniform.
 */
export * from '@workspace/curriculum/catalogs/g10ArabicSem2';
